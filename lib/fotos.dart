import 'package:flutter/material.dart';

class Fotos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
        child: new Center(
          child: new Icon(Icons.add_a_photo, size:150.0, color: Colors.cyan),
        )
    );
  }
}